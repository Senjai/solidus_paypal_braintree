<%# We make this disabled by default, and remove the Spree.disableSaveOnClick(), we manage this differently. %>
<%= submit_tag Spree.t(:save_and_continue), class: 'continue button disabled', disabled: true %>
